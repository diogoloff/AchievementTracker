/* Não foi desewnvolvida */
/* A ideia desta tela era lista em uma lista de checkbox cada objetivo para atingir uma conquista */

import { Text, View } from 'react-native';

export default function Objetivos({ navigation }) {
  return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text style={{ fontSize: 21, marginBottom: 16 }}>Tela de Objetivos</Text>
      </View>
  )
}